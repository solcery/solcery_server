moduleName: 'unityBuilds'
requiredModules:
  api: true

dwellers:
  Api:
    mixins:
      unityBuilds/api: true
      
api:
  paths:
    system:
      commands:
        getUnityBuild:
          params: 
            buildId:
              type: 'string'
              required: true
