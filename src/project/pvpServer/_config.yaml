moduleName: 'project/pvpServer'
requiredModules:
  api: true
  mongo: true
  project/pvpServer/player: true
  project/pvpServer/game: true
  project/pvpServer/matchmaker: true

dwellers:
  Project:
    mixins:
      project/pvpServer/project: true
  Core:
    mixins:
      project/pvpServer/core: true
  Api:
    mixins:
      project/pvpServer/api: true
api:
  game: 
    .params:
      gameId:
        type: 'string'
        required: true
    .commands:
      getGameInfo:
        description: 'Returns game info'
      getGameVersion:
        description: 'Return content version'
        params: 
          version:
            type: 'string'
            description: 'Game version'
                    